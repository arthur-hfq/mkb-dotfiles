-- ╔══════════════════════════════════════════════════════════════════╗
-- ║  [MARKAB] CONKY SWARM — AGENT GLOBE OVERLAY                   ║
-- ║  TRANSPARENT WINDOW / NO CACHE / RAMDISK SOURCE / 1.0s CYCLE  ║
-- ╚══════════════════════════════════════════════════════════════════╝

conky.config = {
    own_window = true,
    own_window_type = 'override',
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 0,
    own_window_colour = '#f4f4f4',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

    alignment = 'middle_middle',
    gap_x = 0,
    gap_y = 0,
    minimum_width = 1000,
    minimum_height = 1000,

    -- ── RENDERING: ramdisk source + no cache = no tearing ────────
    imlib_cache_size = 0,
    double_buffer = true,
    update_interval = 1.0,
    draw_borders = false,
    draw_shades = false,
}

conky.text = [[
${image /dev/shm/nodes.png -p 0,0 -s 1000x1000 -f 1}
]]
